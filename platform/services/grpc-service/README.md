# grpc-service

Roadmap step: 4

API type: gRPC

Purpose: provide an internal gRPC API demo.

Expected behavior:

- Define service contracts in `.proto` files.
- Listen only on the Docker network.
- Test with `grpcurl`.

Status: scaffold only.
