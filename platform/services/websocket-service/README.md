# websocket-service

Roadmap step: 6

API type: WebSocket

Purpose: provide a chat or broadcast demo over WebSocket.

Expected behavior:

- Expose `GET /health`.
- Accept WebSocket upgrade through the gateway route `/ws/*`.
- Broadcast messages between test clients.

Status: scaffold only.
