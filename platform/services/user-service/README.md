# user-service

Roadmap step: 3

API type: REST CRUD

Purpose: manage user records and profile data.

Expected endpoints:

- `GET /health`
- `GET /users`
- `GET /users/:id`
- `POST /users`
- `PUT /users/:id`
- `DELETE /users/:id`

Status: scaffold only.
