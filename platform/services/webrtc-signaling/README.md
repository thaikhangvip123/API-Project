# webrtc-signaling

Roadmap step: 9

API type: WebRTC signaling

Purpose: exchange offer, answer, and ICE candidate messages for a browser 1-1 call demo.

Expected behavior:

- Expose `GET /health`.
- Use a public STUN server from configuration.
- Support signaling through `/webrtc/*`.

Status: scaffold only.
