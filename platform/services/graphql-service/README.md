# graphql-service

Roadmap step: 5

API type: GraphQL

Purpose: expose a unified query layer that calls REST and gRPC services internally.

Expected endpoint:

- `POST /graphql`

Status: scaffold only.
